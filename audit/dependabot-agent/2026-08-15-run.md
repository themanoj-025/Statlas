# Statlas — Dependabot Agent Audit (2026-08-15)

**Repo:** themanoj-025/Statlas · **Ecosystems:** pip, npm (/web), pre-commit
**CI:** pre-commit, pytest + ruff + pip-audit, gitleaks

## Verdicts
| PR | Change | Type | Verdict |
|----|--------|------|---------|
| #1 | ruff >=0.6 → >=0.16.2 | major (toolchain) | AUTO-MERGE-eligible (verified) — pending CI |
| #2 | stripe >=8.0 → >=15.5.0 | major | MERGE-WITH-CAUTION (verified) — pending CI |
| #3 | anthropic >=0.40 → >=0.121.0 | 0.x major | MERGE-WITH-CAUTION (verified) — pending CI (rebase requested; was DIRTY) |
| #4 | pydantic >=2.5 → >=2.13.4 | minor | AUTO-MERGE-eligible (verified) — pending CI |
| #5 | pip-audit >=2.7 → >=2.10.1 | minor | AUTO-MERGE-eligible (verified) — pending CI |
| #6/#7/#8 | typescript 5.9→7.0.2, lucide-react 0.460→1.31, @types/node 22→26 (/web) | major (dev) | HOLD-pending — verify on CI typecheck |
| #9/#10 | pre-commit-hooks v5→6, black 25.9→26.5.1 | pre-commit | verify-on-CI |

## Baseline repair (Phase 0)
- **commit df06e79**: `app/api/billing_views.py` uses pydantic `EmailStr` but `email-validator` was missing from requirements.txt — a fresh `pip install -r requirements.txt` could not import `app.api.main`, breaking pytest collection for every PR. Verified locally: with email-validator installed the full suite passes **131/131**. Added `email-validator>=2.0` to requirements.txt.

## Verification (Phase 2.3)
- Clean venv install resolved to stripe 15.5.0, anthropic 0.122.0, pydantic 2.13.4, ruff 0.16.3, pip-audit 2.10.1 (no resolution errors).
- **pytest: 131 passed** · **ruff check: clean (0 errors)** under ruff 0.16.3 with the repo's pinned select set (E4/E7/E9,F,I,DTZ) · **pip-audit: no known vulnerabilities**.
- Usage audit: stripe — `Customer.create`, `checkout.Session.create`, `billing_portal.Session.create`, `Webhook.construct_event` (stable core API); anthropic — `Anthropic(api_key=...)` (stable constructor).

## Notes
- anthropic PR #3 went DIRTY after the email-validator baseline commit; `@dependabot rebase` requested. All verdicts pending CI (runner recovery).
