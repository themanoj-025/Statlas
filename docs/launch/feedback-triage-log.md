# Statlas — Soft Launch Feedback & Triage Log

*Created: 2026-08-14 (Phase 5 — Part B4/B5). This is the execution record for
the soft launch: what was announced, where, when; every item of feedback with
category, resolution, and response time; and the go/no-go decision against the
pre-defined criteria in `soft-launch-plan.md` §B5.*

---

## Execution record

| Date | Channel | Post/action | Link |
|---|---|---|---|
| (pending) | Twitter/X analytics community | Soft-launch announcement | `launch-post.md` |
| (pending) | r/footballanalysis + r/soccer | Soft-launch announcement | `launch-post.md` |
| (pending) | Analytics Discords | Soft-launch announcement | `launch-post.md` |

The window is **14 days** from the first post. Feedback channels:
`data@statlas.com` (accuracy reports, per-page "Report a data error" links)
and `feedback@statlas.com` (open-ended).

---

## Triage log

Legend — Category: `accuracy` (data value wrong) · `bug` (broken behavior) ·
`feature` (request) · `pricing` · `sentiment`.

| # | Date received | Category | Summary | Resolution | Response time | Status |
|---|---|---|---|---|---|---|
| — | — | — | _No external feedback yet — window opens when the launch post ships._ | — | — | — |

### Response discipline (from the plan, restated here so it is checkable)

- Every `accuracy` item: same/next-day fix + changelog entry + reply to the
  reporter within 24h.
- Every fix made from this log is added to the public changelog as a dated,
  specific entry (Phase 5 C2) — the bug-fixing process is itself visible.
- `feature`/`pricing`/`sentiment` items are read in weekly triage and folded
  into the go/no-go assessment; they do not block.

---

## Go/no-go decision

*To be completed at the end of the 14-day window, against the fixed criteria in
`soft-launch-plan.md` §B5 (not renegotiated here).*

| Criterion | Pass? | Evidence |
|---|---|---|
| No unresolved critical data-accuracy bugs after 14 days | ☐ | (link to triage rows) |
| ≥ 60% positive qualitative sentiment from engaged users | ☐ | (summary) |
| No major infrastructure incidents | ☐ | (summary) |
| ≥ 1 organic free → Pro conversion | ☐ | (Stripe test/prod record) |

**Decision: ☐ GO → wider public launch · ☐ NO-GO → extend with named focus**

Notes:
