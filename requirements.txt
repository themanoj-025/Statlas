# Statlas Phase 1 — data pipeline dependencies
# Production and development. Tests run on SQLite; production targets PostgreSQL.

requests>=2.31,<3.0.0
beautifulsoup4>=4.12,<5.0.0
SQLAlchemy>=2.0,<3.0.0
psycopg2-binary>=2.9,<3.0.0 # PostgreSQL driver for production DATABASE_URL

# API layer (Phase 2) — FastAPI runtime
fastapi>=0.110,<1.0.0
uvicorn>=0.29,<1.0.0
pydantic>=2.13.4,<3.0.0

# Development / tests
pytest>=8.0,<9.0.0
httpx>=0.27,<1.0.0 # fastapi TestClient (tests/test_api.py)
ruff>=0.16.2,<1.0.0 # lint gate (CI + local); enforced set in pyproject.toml
pip-audit>=2.10.1,<3.0.0 # dependency vulnerability scan (C5 closeout; enforced in CI)

# Phase 4 — billing + AI assistant
stripe>=15.5.0,<16.0.0
anthropic>=0.121.0,<1.0.0
email-validator>=2.0,<3.0.0 # pydantic EmailStr (app/api/billing_views.py) — required at import time

# Phase 14 — ML platform (player clustering & archetypes)
scikit-learn>=1.5.0,<2.0.0 # k-means clustering, preprocessing pipeline, evaluation metrics
joblib>=1.3.0,<2.0.0 # model serialization (save/load trained models)
reportlab>=4.0,<5.0.0

# Phase 19 — Production readiness
redis>=5.0.0,<6.0.0 # Redis client for rate limiting, caching, and job queues
alembic>=1.13.0,<2.0.0 # Database migration management
python-json-logger>=3.0.0,<4.0.0 # Structured JSON logging
